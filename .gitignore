# Rust
target/
Cargo.lock.bak
**/*.rs.bk

# wasm-pack intermediate output (we DO commit web/public/pkg/ as a built
# artifact so that the Svelte frontend and GitHub Pages deploys don't need
# a Rust toolchain to produce a working build)
crates/**/pkg/
wasm-pack.log

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local env
.env
.env.local

# Vite + Node
web/node_modules/
web/dist/
web/.vite/
